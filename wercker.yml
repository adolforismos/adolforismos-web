box: debian
build:
  steps:
    - arjen/hugo-build@2.5.0:
        version: "0.48"
        theme: hugo-initio
deploy:
  steps:
    - install-packages:
        packages: firebase
    - dandehavilland/firebase-deploy@1.2.2:
        project: blog-1455c
        token: $FIREBASE_TOKEN
