box: debian
build:
  steps:
    - koldovsky/hugo-build@1.16.5:
      version: "0.24.1"
      theme: "cocoa-eh"
deploy:
    steps:
      - firebase-deploy:
          project: adolforismos-web
          token: $FIREBASE_TOKEN
